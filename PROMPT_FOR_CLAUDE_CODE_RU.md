Ты работаешь в Claude Code внутри этой папки. Прочитай `CLAUDE.md`, затем `TASK.md`, затем `docs/00_READ_ME_FIRST_RU.md`, `docs/01_clean_spec_ru.md`, `docs/02_method_review_ru.md`, `docs/03_validation_cases_ru.md`.

Задача: написать Python-пакет `parametric_ibp_lf_reducer` с нуля и проверяться на validation examples. Не пиши код на Mathematica/Wolfram: Wolfram/Mathematica-style синтаксис в документах — только текстовый формат ввода/вывода и математическая нотация. Начни с архитектуры и тестов, затем реализуй parser, sparse-poly слой, surface/local-finiteness checks, row generation, modular sparse RREF, reconstruction/export и CLI. Нельзя хардкодить конкретные примеры. Нельзя возвращать Success, если `AllLocallyFinite` не True при `epsilon=0`. Работай итеративно: после каждого значимого слоя запускай `python -m pytest` и фиксируй, что осталось.
